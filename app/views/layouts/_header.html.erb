<div class="navbar-wrapper">
  <div class="container-fluid">
    <nav class="navbar navbar-fixed-top navbar-header">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to image_tag("easy.png", alt: "logo", width: "90px"), easybreezys_home_path, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="navbar-collapse collapse pull-centre">

          <ul class="nav navbar-nav pull-right">
            <ul class="nav navbar-nav">
              <li class="active"><%= link_to 'О нас', easybreezys_aboutUs_path, class: "" %></li>
              <li><%= link_to 'Наши работы', easybreezys_ourWork_path %></li>
              <% if user_signed_in? %>
                  <li><%= link_to 'Оставить заявку', new_user_request_path(current_user) %></li>
              <% end %>

            </ul>
            <% if user_signed_in? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %> <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Редактировать профиль', edit_user_registration_path %></li>
                    <li><%= link_to 'Заказы', user_requests_path(current_user) %></li>
                    <li> <%= link_to 'Выйти', destroy_user_session_path, :method => :delete %></li>
                  </ul>
                </li>

            <% else %>
                <li><%= link_to 'Войти', new_user_session_path %></li>
                <li><%= link_to 'Зарегистрироваться', new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </div>
</div>